catkin_create_pkg nome std_msgs roscpp

std-> libstd
roscpp -> com

catkin_make -> dopo la modifica del Makefile dalla cartella ROS

ros/src -> catkin_init_workspace

star/src no ros.


roscore->per avviare il gestore di comm
		.
		V
nella cartella devel usare ". setup.bash" inizializza ambiente di ros per usare i tool sulla finestra su cui lo utilizzo.
	posso usare roscd per accesso rapido ai workspace rispettivi.
	"rosrun talker talker_node" per avviare il talker 
	"rostopic list" list dei topic 
	"rostopic info /nometopic" info sul topic con pub-sub
	"rostopic echo /nometopic" echo del publishing sul topic
	"rqt_graph" grafico delle connessioni



